#include <iostream>

using namespace std;



void main()
{
    cout << !(4 == 5 ) << endl;
}
//Как вы видите прмиер не правильный, но ! делает ответ противоположным, тоесть вместо 0(false) будет 1(true)
//As you can see, the order is not correct, but ! makes the answer the opposite, that is, instead of 0(false) it will be 1(true)
